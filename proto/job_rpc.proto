syntax = "proto3";

package job_rpc;
option go_package="./types/job;job";

message Request {
  string ping = 1;
}

message Response {
  string pong = 1;
}

service Job_rpc {
  rpc Ping(Request) returns(Response);
}
