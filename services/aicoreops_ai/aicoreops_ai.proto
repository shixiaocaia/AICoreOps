syntax = "proto3";

package aicoreops_ai;
option go_package="./aicoreops_ai";

message Request {
  string ping = 1;
}

message Response {
  string pong = 1;
}

service Aicoreops_ai {
  rpc Ping(Request) returns(Response);
}
