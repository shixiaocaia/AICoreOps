syntax = "proto3";

package aicoreops_job;
option go_package="./aicoreops_job";

message Request {
  string ping = 1;
}

message Response {
  string pong = 1;
}

service Aicoreops_job {
  rpc Ping(Request) returns(Response);
}
