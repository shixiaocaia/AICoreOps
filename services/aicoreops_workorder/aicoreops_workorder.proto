syntax = "proto3";

package aicoreops_workorder;
option go_package="./aicoreops_workorder";

message Request {
  string ping = 1;
}

message Response {
  string pong = 1;
}

service Aicoreops_workorder {
  rpc Ping(Request) returns(Response);
}
