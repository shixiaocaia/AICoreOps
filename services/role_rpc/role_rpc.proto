syntax = "proto3";

package role_rpc;
option go_package="./role_rpc";

message Request {
  string ping = 1;
}

message Response {
  string pong = 1;
}

service Role_rpc {
  rpc Ping(Request) returns(Response);
}
